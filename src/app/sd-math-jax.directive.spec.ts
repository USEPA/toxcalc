import { SdMathJaxDirective } from './sd-math-jax.directive';

describe('SdMathJaxDirective', () => {
  it('should create an instance', () => {
    const directive = new SdMathJaxDirective();
    expect(directive).toBeTruthy();
  });
});
